<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardBackgroundColor="@color/tt_dark_surface"
    app:strokeColor="@color/tt_dark_bg_end"
    app:strokeWidth="1dp"
    app:cardCornerRadius="12dp">

    <LinearLayout
        android:padding="12dp"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/row_title"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/tt_dark_accent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/row_sub"
            android:textSize="12sp"
            android:textColor="@color/tt_dark_subtext"
            android:layout_marginTop="4dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/row_msg"
            android:textSize="13sp"
            android:textColor="@color/tt_dark_text"
            android:ellipsize="end"
            android:maxLines="1"
            android:layout_marginTop="6dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
